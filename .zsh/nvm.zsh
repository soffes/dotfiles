export NVM_DIR="$HOME/.nvm"

# Loading NVM greatly reduces tab open time
# [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"

# Alias to load NVM on demand since I don't use it that often.
alias loadnvm="source $NVM_DIR/nvm.sh"
